---
description: Specialist agent usage for {{PROJECT_NAME}} (Agency + .ai/agents.md)
globs: ""
alwaysApply: false
---

# Agents

- Required: pick the Agency specialist per `routing.mdc` and `.ai/agents.md` (planning, architecture, implementation, test, perf, review, security, docs)
- Primary source: **Agency Agents** in `vendor/agency-agents/` — project rules under `.cursor/rules/agency-*.mdc`
- **One** primary specialist per task unless workstreams are independent
- Superpowers (`vendor/superpowers/`) drives workflow phases; Agency drives role execution — both apply by default
