import { dim } from '../cli/logger.js';

/** Placeholder for future `update` command. */
export async function updateCommand() {
  console.log(dim('The `update` command is not implemented yet. Re-run `init` or upgrade the package when available.'));
}
